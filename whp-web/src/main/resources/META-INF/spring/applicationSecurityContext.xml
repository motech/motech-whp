<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

    <beans:import resource="classpath:applicationWebSecurityContext.xml"/>


    <http auto-config="false" use-expressions="true">
        <form-login login-processing-url="/security/j_spring_security_check" login-page="/login"
                    authentication-failure-handler-ref="loginFailureHandler"
                    authentication-success-handler-ref="loginSuccessHandler"/>

        <logout logout-url="/security/j_spring_security_logout" invalidate-session="false" success-handler-ref="logoutSuccessHandler"/>

        <!-- Permit all -->
        <intercept-url pattern="/patient/process" access="permitAll" requires-channel="any"/>
        <intercept-url pattern="/motech-delivery-tools/**" access="permitAll" requires-channel="any"/>

        <!--Only admin-->
        <intercept-url pattern="/adherence/reports/**" access="hasRole('CMF_ADMIN')" requires-channel="any"/>

        <!--Only IT Admin-->
        <intercept-url pattern="/itadmin/**" access="hasRole('IT_ADMIN')" requires-channel="any"/>
        <intercept-url pattern="/cmfadmin/create" access="hasRole('IT_ADMIN')" requires-channel="any"/>

        <!-- Both admin and providers -->
        <intercept-url pattern="/" access="hasAnyRole('PROVIDER','CMF_ADMIN','IT_ADMIN')" requires-channel="any"/>
        <intercept-url pattern="/changePassword" access="hasAnyRole('PROVIDER','CMF_ADMIN','IT_ADMIN')" requires-channel="any"/>

        <!-- Only providers -->
        <intercept-url pattern="/patients/**" access="hasRole('PROVIDER')" requires-channel="any"/>

        <intercept-url pattern="/adherence/**" access="hasRole('PROVIDER')" requires-channel="any"/>

    </http>

    <beans:bean id="loginFailureHandler" class="org.motechproject.security.LoginFailureHandler">
        <beans:constructor-arg value="/login?login_error=t"></beans:constructor-arg>
    </beans:bean>

    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="authenticationProvider"/>
    </authentication-manager>

    <beans:bean id="ajaxAwareAuthenticationProcessingFilterEntryPoint" class="org.motechproject.security.AjaxAwareAuthenticationEntryPoint">
        <beans:property name="loginFormUrl" value="/login"/>
        <beans:property name="forceHttps" value="false"/>
    </beans:bean>

</beans:beans>